import "../styles/learnix.css";

const LearniX = (size) => {
  return (
    <span className={"learnix " + size}>
      Learni<span className="orange">X</span>
      &nbsp;
    </span>
  );
};

export default LearniX;
